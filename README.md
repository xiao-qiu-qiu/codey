# Codey

Codey 是 Codex 桌面客户端的增强启动器。打开 Codey 后，它会自动拉起 Codex，并在 Codex 页面内提供统一的 Codey 控制台，用来管理线路、模型、会话、通知、插件、更新和运行策略。

## 功能描述

- 持续检查 Codey 是否仍能响应；发生异常或连接中断时，Codex 页面右上角的 Codey 图标会显示醒目的异常标识和处理提示。关闭由 Codey 启动的 Codex 后，Codey 也会同步退出。
- 自动启动 Codex，并在页面顶部提供 Codey 控制台入口；可查看运行状态、Codex 版本和应用路径，重启由 Codey 启动的 Codex，并通过控制台顶部的“问题反馈群”入口扫码加入反馈群；还可检查、下载和安装 Codey 更新。打开 Codey 时会先检查更新，等待较久时显示系统提示，最多检查 10 秒；更新源不可用时会自动跳过，发现可安装的新版本时会先询问是否更新。选择稍后会正常进入 Codex，并在 Codey 图标显示更新红点。
- 管理官方登录线路与第三方线路。首次使用时只会自动带入当前可用线路一次；之后第三方线路使用 Codey 保存的数据。每次启动若检测到官方账号登录，或需要交由 Codex 自己确认官方登录状态，会把官方直登作为第一条可用线路；从第三方 API 启动时不会因为保留官方登录而切回官方通道，需要官方模型时请先使用官方线路启动或按界面提示重新启动 Codey。所有第三方线路会同时接入一个本地路由，不需要先切换或启用某条线路；对话中选择模型后，本地路由会按模型所属线路自动分发请求，并保证供应商收到的始终是原始模型 ID。控制台以左右等宽方式展示线路和模型：左侧线路卡片用于管理配置，官方账号卡片直接提供额度显示开关，第三方线路提供纯图标编辑与删除操作；右侧聚合全部供应商模型并按线路分组，官方账号与第三方线路均可在分组头部直接同步模型；整个模型目录只维护一个全局默认模型，可从任意分组设置。模型选择器会用 `[官]` 标记官方账号；第三方线路需配置不超过两个字符的短名称，并以 `[短名称]` 标记所属线路。第三方模型可同步或手动维护，删除线路后对应模型会立即移除。
- Codex 自动风险审核会优先沿用当前任务中明确且支持审核的线路；请求没有线路归属时，优先使用可用的官方账号线路，不会再把内部审核模型当成普通的“未启用模型”拒绝。
- 线路既可手工配置，也可在首次使用时从 Codex 当前 Provider 只读带入；Codey 不会修改、迁移或重写用户已有的 Codex 线路配置。第三方线路的 API Key 默认隐藏，编辑时可点击眼睛按需查看或直接输入新值替换。第三方线路与检测到的官方登录统一接入内置路由，升级前创建的官方任务、从其他设备同步来的旧版自定义线路任务和旧版线路模型标识无需迁移即可继续打开。新增线路、修改接口地址、连接方式或认证信息后会尽量即时生效；同一任务中可即时切换官方模型和任意第三方线路模型，多个线路使用同名模型时仍会准确记住归属，并按所选线路自己的连接方式发送。旧任务缺少可信线路归属时会在发送前停止本次请求，避免误用官方账号或第三方密钥。
- 官方账号线路默认使用长连接加速；第三方线路只有在编辑时明确声明兼容才会开启，其他线路保持常规方式。连接异常时会自动改用常规方式，且不会重复提交已经可能送达的请求。官方账号的长对话经过本地路由时仍可使用服务端压缩，不会再因为同时保存了其他不兼容线路而退回本地压缩；纯第三方线路则按各线路声明的兼容能力启用。
- 只要线路列表中存在可用的官方账号线路并开启额度显示，就会把周额度和可用的 5 小时额度整合到账户区域，不受当前所选模型影响；额度会跟随 Codex 的官方登录状态自动更新，登录刷新或账号切换后会尽快恢复。摘要以标签显示当前套餐，并展示剩余比例与重置时间，鼠标悬浮时可查看各周期用量、Credits 余额和更新时间，也可在控制台关闭。
- 可为任意第三方线路同步可用模型、把其中一个模型设为全局默认并维护自定义模型；模型较多时可搜索并分批浏览。Windows 已信任的内网证书和线路已有的额外鉴权设置也会用于模型同步，首次使用时也可直接保存手动输入的模型。第三方线路的自动审核能力会在同步时识别，也可手动声明；只有明确支持的线路才会接收自动审核请求。第三方线路模型会按模型能力显示推理强度；本机确认支持 Ultra 的 GPT-5.6 模型也可使用自动任务分工，其他模型只有明确支持时才会显示“最高”。第三方线路中的所有模型都只通过该线路调用，即使模型名称与官方模型相同，也不会切换到官方账号通道。保存后会尽量立即刷新 Codex 的模型选择器，失败时按界面提示重启生效。
- 增强会话管理：显示更友好的会话时间，运行中的任务会在各自列表内稳定地优先显示；展开项目时，藏在首批会话之后的运行任务也会自动补充显示并置顶，短暂状态切换时不会立即闪回；其余任务沿用官方的置顶、优先级、最近更新与手动排序，支持导出、导入、删除指定轮次、恢复最近备份；普通用户和助手对话轮次会尽量显示可选操作，便于选择后删除指定内容；启动时会先确认当前线路完整有效，再按该线路修复历史会话归属，并提升历史会话恢复到原项目的稳定性。当任务已经确认结束但页面仍停留在运行状态时，Codey 会自动重新同步当前任务页面。使用仅在 Codey 中保存的第三方线路，不会让历史会话在原版 Codex 中因线路缺失而无法打开；旧版本造成的异常会在下次安全启动时自动修复。
- 增强插件市场与页面体验：修复官方和本地插件展示，支持一键检查并尝试修复插件市场，保存个性化页面增强设置，并可按需屏蔽完全访问模式下反复出现的安全提示（默认关闭）。
- 提供启动与平台体验优化：减少 Codex 卡在启动页的情况；长时间运行时会自动收敛重复遗留的 MCP 与临时执行进程，避免每次工具调用后进程数和内存持续累积，正常的持久工具连接不受影响；Windows 会限制异常密集的后台 Git 状态与审查请求，降低大量短时 Git 进程造成系统资源耗尽的风险，同时阻止 Codex 异常的周期性 WMI 进程采样。保护状态会在启动时完成一次完整自检；自检通过后即显示为已生效，实际拦截次数另行展示，不会为了确认状态持续检测。系统优化详情只列出已经生效的用户功能，当前运行中的内置或已有 FastCtx、子代理优化、正在监听的通知渠道，以及实际生效的 Trace 与 Crashpad 磁盘保护也会在这里汇总；新的生效功能会自动加入列表，渲染器控制等内部基础组件不会混入功能数量，Windows 专属保护也不会在 macOS 中显示为生效。还可选择 Codex 安装目录、显示首次启动失败提示，并提供渲染启动策略用于排查显示异常。
- 支持按需精简宠物功能：默认收起宠物、隐藏宠物入口，并避免在启动时预先创建隐藏的宠物窗口；主动使用官方语音功能时仍可按需启用共用能力，关闭精简后可恢复完整宠物体验。
- 支持上下文工具增强，改善长任务中的文件读取、搜索和替换体验；开启后会批量读取已知文件、先缩小搜索范围并控制结果规模，减少重复调用和不必要的 token 消耗。普通文件读取、文本搜索和文件发现会优先使用 FastCtx，带特殊格式、统计、流式或高级搜索语义的终端命令仍会直接执行，构建、测试、Git 等任务不受影响；工具不可用或失败时可安全回退。共享服务连接意外中断时会自动恢复后续调用，运行中的替换不会被重复执行。内置工具来自 [yc-duan/fastctx](https://github.com/yc-duan/fastctx)；当前 `glob` 更偏向忽略规则，必要时可以显式排除不想看的路径。检测到已经配置自己的 FastCtx，或暂时无法确认其状态时，Codey 会禁用内置工具开关并说明原因，避免重复加载；通过 Codey 启动后，旧版工具提示也会完成一次性迁移，不会在退出时重新出现。线路或模型配置变化不会覆盖这套工具规则。
- 支持提示词优化：可使用 Codey 路由，直接从已启用的所有线路模型中搜索和选择；也可手动填写地址、密钥和模型，并选择 OpenAI 或 Anthropic 接口格式。默认优化指令会直接显示在输入框中，可按需微调，并可随时测试连通性。启用后，Codex 输入框旁会出现「优化」按钮，优化结果会替换原内容并保留在输入框中供继续编辑；配置变更即时生效，API Key 默认隐藏，可点击眼睛按需查看。
- 基于 Codex 原生子代理提供 Codey 角色与调度增强；开启后，单一事实、已知位置修改和顺序任务仍由主代理直接完成；需要多点独立检索、归纳多个候选文件或处理两个独立分支时，会更积极使用只读子代理，较重的独立实现和专门视觉取证也会按任务类型委派。任务规模只作为调度参考，不会因为调用数或文件数估算偏小而被硬性拦截；写入型子代理仍保持较高门槛。已确认的纯只读任务默认最多同时使用三个子代理；批次包含写入型或身份未确认的代理时，最多同时使用两个。每次派生仍会提前检查并行任务的读写范围；上一批完全收拢后，主代理会明确选择继续下一批、转为自己处理、完成任务或报告阻塞；如仍有合适的独立工作，可在同一次回复中继续，不必等待下一条消息。这些协作控制不会扩大其他工具的使用范围；若它们持续不可用，门禁会在有限次数后保守结束本轮并禁止继续派生，而不是反复卡住。派发批次和累计尝试次数不再设置固定预算上限；并发槽释放并完成批次决策后可以在同一次回复中继续，适配长期 Goal 任务。上游隐藏任务正文时，保守兼容模式只派发只读任务；需要写入时会交回主代理，或要求使用可验证的任务契约。正文可见时写入任务仍由主代理原样复跑约定检查，成功才会标记为通过；检查提示会完整显示可复制的原始命令，成功结果也会由 Codex 的可信命令状态正常确认。检查连续失败或长时间无法取得可信结果时，会明确标记为“未验证”并有界释放，不会无限卡住任务，也不会伪装成通过；检测到子代理状态载荷缺少关键字段或结构变化时，也会显示兼容性诊断并要求对账。编排账本保留的兼容计数只用于诊断，不会阻断派发，也不代表实际 token 账单。可分别为“快速定位、深度检索、视觉分析、代码实施、视觉实施”五类专用任务选择所有当前可用线路中已启用的模型和思考深度，包括新版 Codex 支持的轻量子代理模型与第三方模型；保存后下一次派生直接生效。角色的只读或可写设置是默认工作方式，实际权限仍受父任务当前权限模式约束，不会因开启增强而扩大。在完成身份与角色校验后，Codey 不再按任务中声明的目录限制实际读写；子代理能否访问其他工作树或目录，以父任务的 Codex 权限设置为准。首次开启或关闭仍按界面提示重启。线路切换不会替换这套全局角色选择或覆盖协作规则；删除线路时，使用该线路的全局默认模型和子代理角色会自动回退到剩余可用模型，不会继续保留失效引用。子代理不能继续向下派生；主代理会先派完同一批独立任务再等待，局部更新不会被误当成完成，异常代理会先核对并停止，再由主代理接管未完成部分。仍在运行的任务全部只是已确认的只读检索时，主代理可以在等待间隙继续查看和搜索本地文件，先处理已经返回的结果；写入、命令、联网和结束任务仍会等待全部收拢，存在写入、命令或身份未确认的代理时也继续保持原有等待保护。仍在运行的代理可以接收必要补充；已经结束或属于上一轮的代理不会被重新唤醒执行新写入，系统会在唤醒前立即提示改用新任务或由主代理接管，避免运行很久后才在首次修改时失败。正常完成、限流或服务异常都会按对应终态收拢；异常初始化或协作工具意外不可用时，等待保护也会有界释放。用户在代理运行中追加、改变或取消要求时，系统会先重新核对状态；只有被明确取消的子任务会停止，状态询问或补充信息不会误停其他代理。已经完成的结果不会因稍晚到达的停止请求被误记为丢失，被取消请求的迟到结果也不会继续驱动新工作；Codex 异常退出后再次启动时，只恢复尚未完成的写入验收，不沿用已失效的只读任务或等待锁。
- 五类子代理角色可以分别开启或关闭，并保留各自的模型和思考深度选择；快速定位、深度检索和视觉分析为只读角色，代码实施和视觉实施为可写角色。角色启用状态变化需要重启 Codex 后生效。
- 当协作线路会隐藏写入任务正文时，Codey 会先在同一轮中独立登记本次写入的明确工作区根范围和完成检查，再允许对应的写入型子代理启动；登记只能使用一次，过期、跨轮次、跨批次、范围发生变化或信息不一致都会拒绝。这样既能恢复代码实施和视觉实施角色的正常派发，也不会因为看不到任务正文而默认扩大写入权限。
- 只读子代理可直接使用受信任的网页搜索和页面读取能力，不必改用写入型角色或额外声明网络权限；实际联网范围仍以 Codex 当前权限设置为准。需要审查 Git 状态、提交或差异时，只读子代理也可按任务声明运行终端命令，不必伪装成写入型角色；命令能力本身不会开放文件写入，实际副作用仍受角色配置和 Codex 当前权限模式约束。
- 子代理的模型和思考深度使用一套全局角色配置，不会随当前线路切换。模型选择器会聚合所有当前可用线路中已启用的模型，支持按模型或线路搜索并按线路分组；同名模型会保留线路标识，始终通过所选线路调用，不会误走官方账号或其他中转。旧版未带线路信息的选择会优先按原当前线路兼容，只有归属唯一时才自动匹配；已经不可用的值会明确显示，等待用户重新选择。每次启动以及在增强已启用时保存设置，都会核对 Codey 实际交给子代理的完整角色配置；发现任意陈旧、缺失或损坏内容时按当前角色矩阵自动重建，也不会改动用户自己的角色文件。新派生的子代理在首次使用工具前还会核对实际加载的模型和思考深度；无法证明或与角色选择不一致时会暂停该子代理，而不是让它在错误映射上继续工作。
- 当子代理状态载荷只缺少单个标识、但仍能确认调用来源时，主代理不会再在首个子代理启动后误锁住同批后续派发；来源无法确认时仍会保守暂停并提示对账，子代理继续向下派生仍会被拒绝。若模型误用已经派发过的任务名称，系统会先核对原代理并继续等待或交回主代理，不会把去重拒绝误当成任务完成后直接结束。
- Codey 自带的协作与上下文规则会与用户规则分开保存；升级只替换仍保持默认内容的旧版规则，用户修改过的内容会原样保留，避免重复安装造成规则叠加。
- 提供统一的诊断存储统计、清理和保护：可阻止 Trace 日志持续写盘，并在 macOS 上限制待处理崩溃报告异常堆积，帮助避免诊断数据长期占用磁盘。
- 支持最多 32 个飞书机器人（包括企业内网部署）、企业微信 Webhook、Telegram 和微信 ClawBot 通知渠道，可单独启停、删除和测试；微信 ClawBot 可在控制台扫码绑定，扫码确认后按提示在微信中向 ClawBot 发送一条消息即可完成激活，并自动识别当前微信的接收人。配置可直接保存，也可先测试，已保存的地址或登录凭据不会再次显示，可重新扫码替换或显式清除。保存后会立即生效；已激活的微信 ClawBot 会在 Codey 启动后自动恢复连接，并在任务完成、失败或等待介入时发送提醒；服务端暂时限制当前凭据时，Codey 会保留绑定并等待自动恢复，长期无法恢复时仍可重新扫码替换。通知中的模型名称与模型选择器保持一致，使用 `[官]` 或第三方线路的 `[短名称]` 前缀。安装 Codey 更新前会先保存尚未落盘的设置，避免更新重启后丢失。

## 使用方式

打开 Codey 后，它会自动启动 Codex。进入 Codex 后，点击顶部的 “Codey” 按钮即可打开控制台。检测到官方登录时，官方账号模型与第三方线路模型之间的切换会在同一任务中即时生效；第三方 API 启动且没有官方登录态时，只显示可用的第三方线路模型。多数线路新增、删除和模型选择变更也会即时刷新，其他需要重启的设置请按界面提示保存并重启 Codex。

## 注意事项

- Codey 面向 Codex 桌面客户端，不覆盖命令行版本。
- 第三方线路需要选择与服务商匹配的接口类型，官方账号线路的接口类型固定。常用的本地工具、MCP 工具、补丁工具和子代理工具可在线路间自动适配；依赖服务端托管能力或会话状态的专属工具仍需使用原生 Responses 接口，其他不兼容接口则需由对应服务或外部网关转换。
- 远程第三方接口需要使用安全连接，本机服务可继续使用普通本地连接；地址中不要嵌入账号或密码。
- 第三方线路是否可用取决于对应服务本身的能力与账号配置。
- 从第三方 API 启动时不能新增或启用官方账号线路；需要官方模型时，请先在 Codex 完成官方账号登录，再重新启动 Codey。
- 保留官方账号登录只保留 Codex 的账号状态，不会把已选择的第三方线路切回官方接口。
- 删除、导入和恢复类会话操作会尽量保留备份，但仍建议谨慎使用。
- Windows 和 macOS 上启动 Codey 时，如果 Codex 已在运行，Codey 会先将其关闭再重新启动；正在运行的任务会被中断，请提前保存重要内容。
- 部分增强能力依赖当前 Codex 版本和当前线路支持情况，遇到不兼容时请以 Codey 控制台提示为准。线路请求失败时，提示中的请求编号可用于对应诊断信息。
- macOS arm64 未签名构建可能被系统提示为已损坏；请在确认安装包来源可信后按系统提示手动允许，或改用经过签名的发布包。

## 第三方声明

    This product includes FastCtx
    (https://github.com/yc-duan/fastctx), Copyright (c) 2026 yc-duan,
    used under the Apache License 2.0.

    FastCtx is redistributed and/or modified here by the maintainer of
    this distribution. Any such change is that maintainer's own work
    and their sole responsibility. It is not endorsed by, not
    supported by, and not attributable to the author of FastCtx, who
    accepts no liability of any kind arising from this distribution or
    from anything built on top of it.

## 联系方式

Codey 由 [SuperGness](https://github.com/SuperGness) 创建和维护。集成、再分发、合作或任何其他事宜，欢迎联系：kimzane9991@gmail.com。

## 致谢

感谢 [linuxdo](https://linux.do/) 社区的讨论、分享与反馈。
