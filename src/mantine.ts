import { createTheme } from "@mantine/core";

export const codeyMantineTheme = createTheme({
  defaultRadius: "md",
  fontFamily:
    '-apple-system, BlinkMacSystemFont, "SF Pro Display", "Segoe UI", sans-serif',
  primaryColor: "blue",
});
